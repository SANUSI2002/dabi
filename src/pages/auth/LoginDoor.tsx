// Compatibility export for code that still imports the former hospital door.
// Sabi ID now owns authentication and post-login routing for every persona.
export { SignInPage as default } from "@/identity/pages/IdentityPages";
